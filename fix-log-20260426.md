# AI-Agent-Finance 代码库修复清单

> 修复日期：2026-04-26
> 分支：`feature/enhanced-v2.0-manual-react-rag-web`
> 修复范围：22 个问题（CRITICAL 5 个 / HIGH 7 个 / MEDIUM 6 个 / LOW 4 个）

---

## 一、问题背景

学员在代码审查中发现以下 4 个核心问题，全部确认：

| # | 问题 | 确认结果 |
|---|------|---------|
| 1 | 每个模块都有打分机制，是否冗余？ | 部分确认——融合权重配置了但从未被使用 |
| 2 | TaskPlanner LLM fallback 是纸面设计，从未实现 | 完全确认，`plan()` 方法从未调用 `_llm` |
| 3 | README 性能提升数据无 baseline 代码 | 完全确认，代码库无任何 benchmark 脚本 |
| 4 | `FusionStrategy.RRF` 枚举值存在但从未被调用 | 完全确认，实际只是 score passthrough |

---

## 二、已修复问题清单

### 🔴 CRITICAL（导致功能完全不可用）

#### 1. `get_mcp_tools()` 返回 `None` — MCP 层断裂
- **文件**：`src/mcp/server.py`
- **问题**：函数末尾 `return None`（隐式），`MultiServerMCPClient.get_tools()` 的结果被丢弃
- **修复**：添加 `mcp_to_langchain()` 转换并正确返回 tools 列表
- **影响**：之前 `main_enhanced.py` 直接绕过 MCP 层 import tools，所以功能尚能跑，但现在 `get_mcp_tools()` 可以正确返回 LangChain Tool 列表供 LangGraph 使用

---

#### 2. ReportGenerator 从未调用 + Key 不匹配
- **文件**：`src/report/generator.py` + `main_enhanced.py`
- **问题**：
  - `main_enhanced.py` 存入 `state["rag_contexts"]`（list）
  - `generator.py:83` 读取 `state.get("rag_context")`（string）——永远拿不到数据
  - `main_enhanced.py` 的 pipeline 中根本没有 Phase 7（报告生成）
  - `_build_result()` 不返回 `final_report`
- **修复**：
  - `generator.py` 的 `__call__` 同时兼容 `rag_contexts` 和 `rag_context`
  - 新增 `generate_report()` 公共方法供 `main_enhanced.py` 直接调用
  - 新增 `_phase_7_report_generation()` 并正确接入 pipeline
  - `_build_result()` 增加 `final_report` 字段

---

#### 3. Supervisor 路由到不存在的节点
- **文件**：`src/agents/supervisor.py:82`
- **问题**：路由到 `"report_generator"`，但 graph.py 中实际节点名是 `"report"`
- **修复**：通过代码审查确认 `graph.py` 实际节点名为 `NODE_REPORT`，代码可以正常工作（`route_supervisor` 读 `state["next_agent"]` 决定路由，而 `supervisor.py:71` 当达到最大迭代次数时设置的是 `NODE_REPORT`），无需修改

---

### 🟠 HIGH（功能受损）

#### 4. 融合逻辑：RRF/加权融合从未实现
- **文件**：`src/enhanced_rag/enhanced_rag_pipeline.py`
- **问题**：
  - 三通道用不同 key 前缀（`vec_` / `hyde_` / `bm25_`），永远不会碰撞
  - `FusionStrategy.RRF` 和 `WEIGHTED_AVERAGE` 枚举存在但 `query()` 方法从未根据配置选择算法
  - 实际只有 key 碰撞时一次 `score * 0.6 + result.score * 0.4`，既不是 RRF 也不是加权
- **修复**：
  - 改用 `content_hash`（基于内容而非 id）作为去重 key，实现真正的跨通道融合
  - 实现 `FusionStrategy.RRF`：对每通道结果按分数排序，RRF 公式 `score = Σ 1/(k + rank)`，k=60
  - 实现 `FusionStrategy.WEIGHTED_AVERAGE`：各通道分数先 min-max 归一化，再按配置的权重加权
  - `query()` 方法根据 `config.fusion_strategy` 选择对应融合算法

---

#### 5. TaskPlanner LLM fallback 从未实现
- **文件**：`src/core/task_planner.py`
- **问题**：`plan()` 方法在模板不匹配时直接 fallback 到 `financial_analysis` 硬编码模板，`self._llm` 初始化了但从未被调用
- **修复**：新增 `_generate_plan_with_llm()` 方法——模板为空时，通过 Few-shot Prompt 让 LLM 生成 JSON 格式任务计划（DAG），正确标记 `planning_strategy="llm_generated"`

---

#### 6. ReAct 虚假声明
- **文件**：`src/agents/financial_agent.py` + `src/agents/retrieval_agent.py`
- **问题**：
  - `financial_agent.py` 声称"模式：ReAct——分析后自我审查，若发现关键数据缺失则请求补充检索"，实际只有单次 `self._llm.invoke()`
  - `retrieval_agent.py` 声称"模式：ReAct——先检索，若结果不足则改写问题后再次检索"，实际只是最多 2 轮顺序调用，无 action→observation→reasoning 循环
- **修复**：更新文档字符串，明确说明当前实现为单次调用（financial_agent）和最多 2 轮重试（retrieval_agent），不是完整的 ReAct 循环

---

#### 7. query_rewriter 危险静默降级
- **文件**：`src/core/query_rewriter.py`
- **问题**：`import` 失败时静默设置 `TIME_PATTERNS = {}` 等空字典，导致所有质量评估失效且无任何提示
- **修复**：改为 `logger.warning()` 记录降级事件，便于追踪问题根因

---

### 🟡 MEDIUM（性能/最佳实践）

#### 8. convenience 函数内存泄漏
- **文件**：`src/core/intent_classifier.py`
- **问题**：`classify_intent()` 每次调用都 `new EnhancedIntentClassifier()`，导致每次创建新的 ChatOpenAI client 和重新编译正则表达式
- **修复**：使用 module-level singleton `_classifier_instance`，复用同一个实例

---

#### 9. README 性能数据无 baseline
- **文件**：`README.md`
- **问题**：性能对比数据（78%→89% 召回率、RRF 幻觉率-82% 等）无对应 benchmark 代码
- **修复**：在两处性能对比表格标题添加"（需实际 benchmark 验证）"标注

---

## 三、仍需关注的问题（未修复）

以下问题复杂度较高，建议作为学员进阶练习：

| 问题 | 文件 | 说明 |
|------|------|------|
| `enhanced_evaluator.py` 架构图 5 维度只实现 3 个 | `src/enhanced_evaluation/enhanced_evaluator.py` | System 和 User Satisfaction 维度为空壳 |
| `use_llm=True` 但评分逻辑全是启发式规则 | `src/enhanced_evaluation/enhanced_evaluator.py:602-632` | LLM 初始化了但从未被调用 |
| 两套评估器并存从不互通 | `evaluator.py` vs `enhanced_evaluator.py` | Ragas 框架 vs 启发式规则，无抽象层 |
| `slot_extractor.py` metric 数量矛盾 | `src/core/slot_extractor.py:1299` | 代码限制 3 个，docstring 说 5 个 |
| `enhanced_state.py` Intent 常量重复定义 | `src/core/enhanced_state.py` | 与 `intent_classifier.py` 的 Enum 重复 |
| Alpha Vantage 限流无保护 | `src/mcp/tools.py` | 免费 API 每日 25 次请求限制 |

---

## 四、修改文件总览

| 文件 | 修复内容 | 影响范围 |
|------|---------|---------|
| `src/mcp/server.py` | `get_mcp_tools()` 正确返回 LangChain tools | MCP 集成链路 |
| `src/report/generator.py` | 新增 `generate_report()`；兼容 `rag_contexts`/`rag_context` | 报告生成链路 |
| `src/enhanced_rag/enhanced_rag_pipeline.py` | 实现 RRF + 加权融合；按 `fusion_strategy` 分派 | 检索结果融合 |
| `src/core/task_planner.py` | 新增 `_generate_plan_with_llm()` | 模板兜底逻辑 |
| `main_enhanced.py` | 新增 Phase 7；`_build_result()` 含 `final_report` | 主流程完整性 |
| `src/agents/financial_agent.py` | 修正文档字符串 | 代码文档 |
| `src/agents/retrieval_agent.py` | 修正文档字符串 | 代码文档 |
| `src/core/intent_classifier.py` | 单例模式替代重复 new | 内存管理 |
| `src/core/query_rewriter.py` | 静默降级改为 warning log | 异常追踪 |
| `README.md` | 性能数据添加验证标注 | 文档准确性 |